import { Container } from '../../common'

const ResetScreen = () => {
  
  return(
  	<></>
  )
}

const Reset = (props) => {
  
  return(
  	<Container screen={<ResetScreen />} />
  )
}

export default Reset